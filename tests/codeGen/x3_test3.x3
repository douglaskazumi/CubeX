# Cubex Compiler Test 3 - Stage 3

fun rest<T>(l : Iterable<T>) : Iterable<T>
{
	ret := [];
	passed := false;
	for(e in l)
	{
		if(passed)
		{
			ret := ret ++ [e];
		}
		passed := true;
	}
	return ret;
}

fun last<T>(l : Iterable<T>, default : T) : T
{
	for(e in l)
	{
		return last<T>(rest<T>(l),e);
	}
	return default;
}

t1:=last<Integer>(12..13,0);

return [""];