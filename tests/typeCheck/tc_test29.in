class Bb<T>(x : T)
{
	super();
	
	fun getT (t : T) : Iterable<T> = [t];
	
	fun tasticb() : Iterable<T>
	{
		
	
		if(true)
		{
			return getT(x);
		}
		else
		{
			return getT(x);
		}
	}
}

return Bb<String>("hi").tasticb();